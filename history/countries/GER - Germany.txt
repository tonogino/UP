capital = 64

set_politics = {
	ruling_party = neutrality
	last_election = "1936.1.1"
	election_frequency = 48
	elections_allowed = no
}

set_popularities = {
	neutrality = 80
	democratic = 5
	fascism = 10
	communism = 5
}

create_country_leader = {
	name = "Wilhelm II"
	picture = "portrait_GER_wilhelm_iv.dds"
	expire = "1965.1.1"
	ideology = despotism
	traits = {
	}
}

# Berlin guarantees the post-war Habsburg settlement.
give_guarantee = ILY

# The eastern settlement created after the German victory.
# Keep this setup here so vanilla diplomacy cannot reassert itself.
if = {
	limit = {
		OR = {
			has_dlc = "Together for Victory"
			has_dlc = "Man the Guns"
		}
	}
	set_autonomy = { target = POL autonomous_state = autonomy_puppet }
	set_autonomy = { target = UKR autonomous_state = autonomy_puppet }
	set_autonomy = { target = LIT autonomous_state = autonomy_puppet }
	set_autonomy = { target = LAT autonomous_state = autonomy_puppet }
	set_autonomy = { target = EST autonomous_state = autonomy_puppet }
	else = {
		puppet = POL
		puppet = UKR
		puppet = LIT
		puppet = LAT
		puppet = EST
	}
}
